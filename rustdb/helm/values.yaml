replicaCount: 1

image:
  repository: rustdb
  pullPolicy: IfNotPresent
  tag: "latest"

service:
  type: ClusterIP
  port: 5432

resources:
  limits:
    cpu: 2000m
    memory: 2Gi
  requests:
    cpu: 1000m
    memory: 1Gi

persistence:
  enabled: true
  size: 10Gi
  storageClassName: "standard"
